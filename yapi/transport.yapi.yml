yapi: v1
# Control Ardour transport
rpc: ApplyEdits

body:
  undo_step_name: "Transport control"
  operations:
    - transport:
        type: TRANSPORT_ACTION_TYPE_JUMP_TO_BEAT
        double_value: 4.0

expect:
  assert:
    - .success == true
